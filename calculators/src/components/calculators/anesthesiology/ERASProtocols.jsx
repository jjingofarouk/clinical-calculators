
import React from 'react';
import { Box, Typography } from '@mui/material';

const ERASProtocols = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h6">ERASProtocols</Typography>
      <Typography variant="body2">
        This calculator is under development.
      </Typography>
    </Box>
  );
};

export default ERASProtocols;
